arazzo: 1.2.0
info:
  title: Invalid gRPC streaming request-item field
  version: 1.0.0
sourceDescriptions:
  - name: grpcLibrary
    type: protobuf
    url: ./grpc-library.proto
workflows:
  - workflowId: invalid
    steps:
      - stepId: session
        grpcMethod: $sourceDescriptions.grpcLibrary.org.v1.Library/UploadChunks
        requestBody:
          - payload:
              type: START
            # Invalid: streaming request items only allow Request Body Object fields.
            notAStreamingRequestField: true
