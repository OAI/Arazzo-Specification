arazzo: 1.2.0
info:
  title: Interaction step with an invalid mode value
  version: 1.0.0
sourceDescriptions:
  - name: userAPI
    url: https://users.com/openapi.yaml
    type: openapi
workflows:
  - workflowId: approvalWorkflow
    steps:
      - stepId: await-approval
        interaction:
          mode: approval
          prompt: "Please approve."
          inputSchema:
            type: object
            required: [approved]
            properties:
              approved:
                type: boolean
