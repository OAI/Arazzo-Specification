arazzo: 1.2.0
info:
  title: Invalid Protocol Buffer source-description type alias
  version: 1.0.0
sourceDescriptions:
  - name: grpcLibrary
    # Invalid: grpc is not a Protocol Buffer source-description type; use protobuf.
    type: grpc
    url: ./grpc-library.proto
workflows:
  - workflowId: invalid
    steps:
      - stepId: getBook
        grpcMethod: $sourceDescriptions.grpcLibrary.org.v1.Library/GetBook
