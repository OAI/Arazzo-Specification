arazzo: 1.2.0
info:
  title: GraphQL operation requires document
  version: 1.0.0
sourceDescriptions:
  - name: shopSchema
    type: graphql
    url: ./schema.graphqls
workflows:
  - workflowId: invalidGraphql
    steps:
      - stepId: missingDocument
        graphqlOperation:
          schema: $sourceDescriptions.shopSchema
