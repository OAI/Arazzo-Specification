arazzo: 1.2.0
info:
  title: GraphQL extensions selector must be valid
  version: 1.0.0
sourceDescriptions:
  - name: shopSchema
    type: graphql
    url: ./schema.graphqls
workflows:
  - workflowId: lookupBook
    steps:
      - stepId: getBook
        graphqlOperation:
          schema: $sourceDescriptions.shopSchema
          operation: 'query GetBook { book { id } }'
          extensionsSelector:
            context: $inputs.request
            selector: $.extensions
